[package]
name = "tsunami-serve"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "tsunami-serve"
path = "src/main.rs"

[dependencies]
tsunami-core = { path = "../tsunami-core" }
rmcp = { version = "0.16", features = ["server", "transport-io"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "1"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
